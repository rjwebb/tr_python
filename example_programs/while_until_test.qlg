thing ::= asteroid | something

direction ::= left | right | centre | dead_centre

durative turn_right : (),
         turn_left : (),
         move_forward : (),
         move_backward : (),
         nothing : ()

discrete shoot : ()

percept facing_direction : (num),
        see : (thing, direction, num)



top_task(){
see(asteroid, Dir, Dist) & Dist < 10 while Dist < 100 min 3 ~> move_forward
see(dshfjkdhfd, dfsdf, sdfkfdjksf) while A + 1 > 2 min 323 until see(hfjdhfd, jsdfd, dhjdfh) min 3333 ~> move_backward
}